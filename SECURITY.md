# Security

- 默认禁止 Headless Chrome/Edge 联网：模板不含远程资源，渲染可信本地 HTML 时不会外传内容。
- 只渲染你信任的 HTML。默认断网挡的是网络外传，不挡页面脚本在本机的行为；渲染陌生文件前应先查看内容。
- 默认启用浏览器沙箱。脚本会用最小探针检测沙箱是否可用；只有带沙箱无法启动、去掉沙箱能启动时才自动降级，并在终端 stdout 显示 WARN。`OVS_NO_SANDBOX=1` 可强制关闭沙箱，仅用于容器/CI 等必要场景。
- 临时文件使用唯一名称，并在正常和异常路径下清理；若进程被强杀，可能残留 `%TEMP%\ovs-*` 或 `office-visual-spec-print-*`，可手动删除。
- 本包不采集遥测、不读取个人文件、不包含任何密钥或远程回传逻辑。
